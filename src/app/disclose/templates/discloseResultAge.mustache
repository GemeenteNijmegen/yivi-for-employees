{{>header}}
<link rel="stylesheet" href="static/styles/yivi.min.css">

<main>
    <!-- START: template component(s) -->
    <div class="container" id="section-1">
        <div class="row">
            <div class="col-12">
                <h1>Ben je ouder dan 18?</h1>
                <p class="lead">Zonder je geboortedatum te delen is er bepaald of je ouder bent dan 18.</p>
                    {{#error}}
                    <div class="error"> 
                        <h1>Er is iets fout gegaan</h1>
                        <p class="lead">{{error}}</p>
                    </div>
                    {{/error}}
                <h1>Resultaat: {{result}}</h1>
            </div>
        </div>
    </div>
    <!-- END: template component(s) -->
</main>

{{>footer}}
